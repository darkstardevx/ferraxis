# Lexer Corpus

This directory is reserved for corpus-style lexer inputs and golden expectations. Phase 0 executable
regression tests currently live in the lexer and driver crates while the corpus harness is built.
